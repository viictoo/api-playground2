name: Simple Dispatch
on:
  workflow_dispatch:
    inputs:
      name:
        description: 'A name'
        required: false
        default: 'World'
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello ${{ github.event.inputs.name }}"
